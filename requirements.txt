# =============================================================================
# Worldview Database System — Requirements
# =============================================================================
# Verified environment: Python 3.10.11, Windows x64
# Compatible: Python 3.10 ~ 3.13
# Last verified: 2026-06-20
#
# ALL versions are pinned to exact numbers known to work together.
# If you upgrade one, upgrade all simultaneously.
#
# ── Virtual Environment Setup ──
#
#   Windows PowerShell:
#     .\venv\Scripts\Activate.ps1
#     pip install -r requirements.txt
#     python main.py
#
#   Windows (if execution policy blocks):
#     .\venv\Scripts\python.exe -m pip install -r requirements.txt
#     .\venv\Scripts\python.exe main.py
#
#   Linux/Mac:
#     source venv/bin/activate
#     pip install -r requirements.txt
#     python main.py
#
#   Without venv (system Python):
#     pip install -r requirements.txt
#     python main.py   # Auto-detects and adds system site-packages
# =============================================================================

# ── Core Web Framework ──
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.2
jinja2==3.1.2
python-multipart==0.0.6

# ── LLM Integration (HTTP) ──
requests==2.34.2

# ── Vector Store (RAG pipeline) ──
sqlite-vec==0.1.9

# ── Config & Utilities ──
PyYAML==6.0.3

# ── Optional Accelerators ──
# httptools==0.8.0        # uvicorn HTTP parsing accelerator
# watchfiles==1.2.0       # uvicorn --reload file watcher
