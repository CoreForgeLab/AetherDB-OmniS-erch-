# =============================================================================
# Worldview Database System — Requirements
# =============================================================================
# Verified environment: Python 3.10.11, Windows x64
# Supported: Python 3.10 ~ 3.13
# Last verified: 2026-06-20
#
# ALL versions are pinned to exact numbers known to work together.
# If you need to upgrade a package, upgrade all simultaneously.
# For troubleshooting: start by matching these exact versions.
# =============================================================================

# --- Core Web Framework ---
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.2
starlette==0.27.0          # TemplateResponse compat: 0.27.0 old-sig / 1.3.1+ new-sig
jinja2==3.1.2
python-multipart==0.0.6

# --- LLM Integration (optional — only needed for LLM extraction) ---
# openai>=1.0.0            # Uncomment to use OpenAI / DeepSeek / compatible APIs
requests==2.34.2

# --- Vector Store (RAG pipeline) ---
sqlite-vec==0.1.9

# --- Utilities ---
PyYAML==6.0.3
httptools==0.8.0           # Optional: uvicorn HTTP parser accelerator
